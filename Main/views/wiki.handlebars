    <h1>{{name}}</h1>
    <h2>{{description}}</h2>
    <div>
        <iframe src="{{wiki_url}}" width="80%" height="500" style="border:none;">
        </iframe>
    </div>
    
    <div>
        <button>
            Add to Favorite
        </button>
        <ul>
            {{#each user_data as |user|}}

            <li>
                {{user_name}}
            </li>

            {{/each}}

            
        </ul>

    </div>
